<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!-- 主题 -->
    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="actionOverflowMenuStyle">@style/OverflowMenuStyle</item>
        <item name="android:actionOverflowButtonStyle">@style/OverflowMenu</item>
        <item name="textAppearanceLargePopupMenu">@style/OverflowMenuTextStyle</item>
        <!-- Activity转场动画 -->
        <item name="android:windowContentTransitions">true</item>
    </style>
    <!--OverflowMenu 图片设置-->
    <style name="OverflowMenu" parent="@android:style/Widget.ActionButton.Overflow">
        <item name="android:src">@mipmap/main_add</item>
    </style>
    <!--OverflowMenu 弹出框位置设置-->
    <style name="OverflowMenuStyle" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>
        <item name="android:popupBackground">@color/colorPrimaryDark</item>
    </style>

    <style name="OverflowMenuTextStyle" parent="Base.TextAppearance.AppCompat.Light.Widget.PopupMenu.Large">
        <item name="android:textColor">@color/colorAccent</item>
    </style>
    <declare-styleable name="MaterialMenuView">
        <attr name="mm_color" format="color"/>
        <attr name="mm_visible" format="boolean"/>
        <attr name="mm_scale" format="integer"/>
        <attr name="mm_transformDuration" format="integer"/>
        <attr name="mm_strokeWidth" format="integer"/>
        <attr name="mm_rtlEnabled" format="boolean"/>
        <attr name="mm_iconState" format="enum">
            <enum name="burger" value="0" />
            <enum name="arrow" value="1" />
            <enum name="x" value="2" />
            <enum name="check" value="3" />
        </attr>
    </declare-styleable>

</resources>
